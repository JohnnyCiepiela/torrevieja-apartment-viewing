/*
 * The website does not require JavaScript at the moment.
 *
 * This file is intentionally kept so that you have a place
 * for small interactions later without changing the structure.
 *
 * Examples of things you could add later:
 * - fullscreen gallery
 * - image lightbox
 * - navigation
 * - subtle scroll effects
 * - custom Gaussian splat controls
 */

document.addEventListener("DOMContentLoaded", () => {
  // Ready for future enhancements.
});